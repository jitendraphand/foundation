/**
 * The one thing you must edit for the GitHub Pages landing button to work.
 *
 * Set SERVER_URL to your Oracle Cloud instance address.
 *
 * With no domain name, use sslip.io: take the instance's public IP and replace
 * the dots with dashes.
 *
 *     Public IP 132.145.10.20   ->   https://132-145-10-20.sslip.io
 *
 * This MUST be the same value as PUBLIC_HOST in the server's .env file, and it
 * must start with https:// so that browsers do not warn about a mixed
 * secure/insecure transition.
 *
 * Commit and push this file; GitHub Pages redeploys within about a minute.
 */
window.FOUNDATION_CONFIG = {
  SERVER_URL: 'https://80-255-199-192.sslip.io',
};
